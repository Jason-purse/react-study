import React, {memo} from 'react';
import {HeadWrapper} from "./style";

export default memo(() => {
    return (
        <div>
            <HeadWrapper>

            </HeadWrapper>
        </div>
    );
});