import {createSlice} from "@reduxjs/toolkit";

let slice = createSlice({
    name: 'entire',
    initialState: {
        name: 'entire'
    },
    // actions
    reducers: {}
});
export default slice.reducer